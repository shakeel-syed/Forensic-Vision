services:
  forensic-api:
    build: .
    container_name: forensic-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: always

  # Optional: Placeholder for the .NET Dashboard
  # forensic-dashboard:
  #   build:
  #     context: ./ForensicDashboard
  #     dockerfile: Dockerfile # You would need to create this
  #   ports:
  #     - "5000:80"
  #   depends_on:
  #     - forensic-api
